# Gradle / KMP build outputs
.gradle/
build/
.kotlin/
local.properties

# IDE
.idea/
*.iml

# JS / Wasm (yarn + kotlin-js-store)
node_modules/
kotlin-js-store/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Local tooling
.pi/

# Python
__pycache__/
*.py[cod]
# Local publish credentials (never commit)
*credentials*
secring.*
*.gpg
